const { initPluginOptions } = require('./options');
const {
  initWebMonetizationAPI,
  simulateWebMonetization
} = require('./monetization');

module.exports = {
  initPluginOptions,
  initWebMonetizationAPI,
  simulateWebMonetization
};
